;load the starting values
ldi r1 3    ; x pos (start at 1 to avoid r0=0 lower bound edge case)
ldi r2 1    ; y pos
ldi r3 2    ; x velocity
ldi r4 1    ; y velocity
ldi r5 36   ; upper bound (0-indexed, so 31 = last pixel on 32px screen)
ldi r6 64

.loop

; clear the last pixel
io r1 1 write
io r2 2 write
io r0 3 write

add r1 r3 r1
add r2 r4 r2

sub r1 r6 r0
brn return1
call reverseX
ldi r1 64
.return1

sub r0 r1 r0
brn return2
call reverseX
ldi r1 1
.return2

sub r2 r5 r0
brn return3
call reverseY
ldi r2 36
.return3

sub r0 r2 r0
brn return4
call reverseY
ldi r2 1
.return4

ldi r7 1
io r1 1 write
io r2 2 write
io r7 3 write
io r7 4 write
xor r7 r7 r7

jump loop

hlt

.reverseX
sub r0 r3 r3
rtn

.reverseY
sub r0 r4 r4
rtn